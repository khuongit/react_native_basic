import { View, Text, StyleSheet } from "react-native";

const style = StyleSheet.create({
    header: {
        fontSize: 20,
        textAlign: 'center',
        margin: 2,
        color: 'white',
        fontWeight:"bold"

    }
})

export default style;